#include "iostream"
#include "stdlib.h"
#include "string.h"
using namespace std;
int main(){
    char c[100];
    gets(c);
    char* token  = strtok(c," ");
    char a[10][100];
    while (token!=NULL)
    {
        // cout<<token<<" ";
        // token = strtok(NULL," ");
        if(strcmp())
    }
            
}